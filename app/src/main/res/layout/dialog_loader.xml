<?xml version="1.0" encoding="utf-8"?>
<!--
  dialog_loader.xml
  Description:
  A centered loader dialog used for social sign-in transitions (Google, Facebook)
  to prevent UI from appearing stuck before navigating to Dashboard.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="250dp"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp"
    android:background="@drawable/loader_dialog_bg"
    android:gravity="center"
    android:elevation="8dp">

    <ProgressBar
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:indeterminate="true"
        android:layout_gravity="center" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Signing you in..."
        android:textSize="16sp"
        android:textColor="@color/black"
        android:layout_marginTop="16dp"
        android:layout_gravity="center" />
</LinearLayout>
